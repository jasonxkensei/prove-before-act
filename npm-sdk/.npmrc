# Auth token for publishing is injected by setup-node@v4 (registry-url) in CI.
# Do not add _authToken here — it breaks npm install on environments where the
# token env var is not set (e.g. GitHub Actions test jobs).
